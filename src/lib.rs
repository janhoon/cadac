mod args;
mod cli;
mod parser;

pub fn exec_cadac() {
    // let sql = parse_sql("SELECT a, b, c FROM users");
    // println!("{:?}", sql.name);
    // let args = BaseCliArgs::parse();
    // main_cli().unwrap();
}
